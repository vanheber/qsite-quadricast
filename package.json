{
  "name": "qsite",
  "version": "4.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "qsite build:all",
    "build:site": "qsite build:site",
    "build:blog": "qsite build:blog",
    "build:podcast": "qsite build:podcast",
    "build:seo": "qsite build:seo",
    "build:bios": "qsite build:bios",
    "build:catalog": "qsite build:catalog",
    "build:router": "qsite build:router",
    "build:all": "qsite build:all",
    "deploy": "qsite deploy",
    "update": "qsite update",
    "preview": "npx serve public",
    "lint": "npx eslint . --max-warnings=100",
    "lint:fix": "npx eslint . --fix"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bootstrap": "5.3",
    "bootstrap-icons": "^1.13.1",
    "ejs": "^3.1.10",
    "front-matter": "^4.0.2",
    "fs-extra": "^11.3.2",
    "gray-matter": "^4.0.3",
    "marked": "^12.0.2",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "dotenv": "^17.2.3",
    "eslint": "^10.6.0",
    "qsite-core": "file:../qsite-core",
    "ssh2-sftp-client": "^12.0.1"
  }
}
